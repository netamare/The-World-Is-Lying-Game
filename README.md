# The World Is Lying

A browser based investigation game built with React, TypeScript, and Vite. Inspect each scene, find its anomalies, and uncover the signal.

## Run locally

```bash
npm install
npm run dev
```

Open the local URL printed by Vite. To create a production build, run `npm run build`.
